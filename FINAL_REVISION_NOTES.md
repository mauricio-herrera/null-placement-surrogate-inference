# Final revision notes

## Scientific status

The manuscript now has four distinct evidence layers, explicitly separated in the text:

- formal null-transport/lumpability theory and analytic counterexample;
- prespecified synthetic confirmatory benchmark and independent probability-equalization experiment;
- post hoc Gaussian/operator-ablation and within-fiber mechanism diagnostics;
- prospectively frozen SINCA observational application, whose cohort and inferential protocol were fixed before application-level surrogate outputs were inspected.

## Applied conclusion

The SINCA result is substantially more discriminating than the previous Amazon illustration. Under the fixed PM2.5 threshold, the index-level null is displaced strongly toward Ferro-Segers theta=1 relative to the native transported null. The applied data do **not** support a universal null-width-contraction mechanism, and this negative prespecified result is retained prominently in the manuscript and supplement.

## Remaining administrative step before journal submission

The existing Zenodo synthetic archive should be versioned to include the SINCA frozen cohort/protocol, application scripts and outputs, and the new final manuscript figures. No scientific analysis decision remains open in this V1 final package.
